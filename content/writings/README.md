# Writing

A few posts in the pipeline. Mostly drafts for now — RSS coming when the ink dries.

    → agentic-ai-field-notes.md   [drafting]
    → pi-zero-vpn-gateway.md       [drafting]

This is a real directory — `cd writings`, then `ls`, then `cat` (or `less`) a post.
